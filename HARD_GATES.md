# 高风险场景强制门禁

以下场景必须自动进入 `strict` 严格模式，不能为了速度跳过测试、风险、回滚和验收。

## 必须进入严格模式的场景

- 支付、退款、订单状态流转、余额、积分、库存扣减、优惠券核销
- 登录认证、Token / Session、验证码、密码、账号安全
- 权限系统、RBAC、菜单权限、接口权限、数据权限、后台管理
- 文件上传、私有文件下载、第三方存储
- 数据库变更、生产库迁移、数据删除、备份恢复
- 生产部署、灰度发布、回滚、线上 Bug、Hotfix
- 用户隐私数据、敏感信息、日志脱敏、合规相关数据

## 严格模式必须输出

| 必填产物 | 说明 |
|---|---|
| 风险清单 | 明确风险、影响范围、触发条件、严重程度 |
| 测试用例 | 正常、边界、异常、权限、并发/重复请求 |
| 回滚方案 | 代码、数据库、配置、第三方依赖如何回退 |
| 验收标准 | 可检查、可执行、可判定通过/不通过 |
| 日志与监控点 | 关键链路日志、告警、排查入口 |
| 失败处理方案 | 超时、失败、重复、部分成功、补偿机制 |

## 数据库变更专用门禁

每次数据库变更必须包含：

1. 变更目的
2. 影响表、字段、索引、约束
3. 兼容性分析
4. 迁移 SQL
5. 回滚 SQL
6. 测试数据与测试步骤
7. 备份方案
8. 上线步骤
9. 验证 SQL
10. 失败后的恢复策略

## 订单支付专用门禁

必须包含：订单状态机、支付单状态机、回调验签、幂等键、金额以后端为准、重复回调处理、超时未支付关闭、退款流程、对账逻辑、异常补偿。

## 权限专用门禁

必须同时检查：前端路由守卫、后端接口权限、数据归属、角色边界、后台接口默认关闭、越权测试用例。
